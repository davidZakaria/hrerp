# =============================================================================
# HR-ERP Production Environment Configuration
# =============================================================================
# Copy this file to .env and update with your actual values
# NEVER commit the .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED VARIABLES (application will not start without these)
# -----------------------------------------------------------------------------

# Environment
NODE_ENV=production
PORT=5000

# Database Configuration
# For production, use a secure connection string with authentication
MONGODB_URI=mongodb://username:password@localhost:27017/hr-erp?authSource=admin

# JWT Secret - CRITICAL: Generate a strong random string (64+ characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=GENERATE_A_STRONG_RANDOM_STRING_AT_LEAST_64_CHARACTERS

# CORS - Comma-separated list of allowed origins (your frontend URLs)
CORS_ORIGIN=https://your-domain.com,https://www.your-domain.com

# -----------------------------------------------------------------------------
# RECOMMENDED VARIABLES (important for full functionality)
# -----------------------------------------------------------------------------

# Application URLs
FRONTEND_URL=https://your-domain.com
BACKEND_URL=https://your-domain.com

# Backup System - Generate a strong encryption key for encrypted backups
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BACKUP_ENCRYPTION_KEY=GENERATE_A_32_BYTE_HEX_STRING_FOR_BACKUP_ENCRYPTION

# -----------------------------------------------------------------------------
# OPTIONAL VARIABLES (for additional features)
# -----------------------------------------------------------------------------

# Email Configuration (for password reset and notifications)
# For Gmail, use an App Password: https://support.google.com/accounts/answer/185833
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# -----------------------------------------------------------------------------
# SECURITY CHECKLIST
# -----------------------------------------------------------------------------
# [ ] Generated strong JWT_SECRET (64+ characters)
# [ ] Generated BACKUP_ENCRYPTION_KEY (32 bytes)
# [ ] Updated MONGODB_URI with secure credentials
# [ ] Set CORS_ORIGIN to your actual domain(s)
# [ ] Set FRONTEND_URL and BACKEND_URL to your actual domain
# [ ] Configured email settings if using password reset
# [ ] This file is NOT committed to git (check .gitignore)
