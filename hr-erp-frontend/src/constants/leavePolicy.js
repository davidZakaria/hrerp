/** Company leave policy quotas (annual / casual). */
export const ANNUAL_LEAVE_QUOTA = 15;
export const CASUAL_LEAVE_QUOTA = 6;

/** Warn when balance falls at or below ~20% of quota. */
export const LOW_ANNUAL_BALANCE = 3;
export const LOW_CASUAL_BALANCE = 2;
