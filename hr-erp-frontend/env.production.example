# HR-ERP Frontend Production Environment Configuration
# =====================================================
# Copy this file to .env.production (add the dot at the beginning)
# and update the values before building for production.
#
# Rename command: copy env.production.example .env.production

# API URL - The URL where your backend API is hosted
# This should be your production domain (with HTTPS in production)
REACT_APP_API_URL=https://your-domain.com

# Example configurations:
# - If using same domain: REACT_APP_API_URL=https://example.com
# - If using subdomain: REACT_APP_API_URL=https://api.example.com
# - If backend is on different port: REACT_APP_API_URL=https://example.com:5000

# IMPORTANT: 
# 1. Do NOT include trailing slash
# 2. In production, always use HTTPS
# 3. Make sure CORS is configured on the backend to allow this origin
